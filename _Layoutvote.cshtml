<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>eVoting App</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        html, body {
            height: 100%;
            margin: 0;
            background-color: #ffffff; /* White body */
            color: #004d66;
            display: flex;
            flex-direction: column;
        }

        header, footer {
            background-color: #33c3cc; /* Tail blue from original */
            color: white;
            padding: 15px 0;
            text-align: center;
            flex-shrink: 0;
        }

        .wrapper {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 40px 20px;
            text-align: center;
            background-color: #ffffff;
        }

        .language-btn {
            font-size: 1.25rem;
            padding: 15px 30px;
            margin: 15px;
            border: none;
            border-radius: 8px;
            background-color: #e6f7f9;
            color: #004d66;
            transition: background-color 0.3s;
        }

            .language-btn:hover {
                background-color: #cceeee;
                color: #003344;
            }
    </style>
</head>
<body>
    <header>
        <h1 class="h3 mb-0">eVoting App</h1>
    </header>

    <div class="wrapper">
        @RenderBody()
    </div>

    <footer>
        <small>© 2025 eVoting App. All rights reserved.</small>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
